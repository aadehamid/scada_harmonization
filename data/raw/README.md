# data/raw — local scratch (gitignored)

Not the durable warehouse. Hamid keeps raw on R2 `lagos-chem-l0`
(1,419,880,076 bytes: four TEP `.RData` files + IIoT zip, no csv). There is no
"keep raw" rule on the lab disk. NAB skipped for Phase 1 exit. IIoT is snapshot
per machine, not 1 s. See `design/PHASE1_SYNTHETIC_DATA.md` and
`design/PHASE1_DATASHEET.md`.

<<<<<<< HEAD
# Skeleton CI — ruff + ty + the import smoke test. Grows when domain tests land.
=======
# Golden-slice CI — ruff + ty + pytest. No network, no full-dataset jobs.
>>>>>>> e18ca9b (fix: make Phase 1 L0 code ty-clean and run ty in CI)
name: ci

on:
  push:
    branches: [main]
  pull_request:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v10.0.1
        with:
          enable-cache: true
      - name: Sync (dev group)
        run: uv sync --group dev
      - name: Ruff
        run: uv run ruff check .
<<<<<<< HEAD
      - name: ty
        run: uv run ty check
=======
      - name: Ty
        run: uv run ty check src tests
>>>>>>> e18ca9b (fix: make Phase 1 L0 code ty-clean and run ty in CI)
      - name: Pytest
        run: uv run pytest

# generation (Phase 1)

Seeded synthetic sources that are not benchmark downloads. The 1 s fast
class (charter §14 N8) is `generate_machine_stream` — a wide IIoT frame
with native UTC. `ingest_iiot` melts it. Machine identity stays metadata.
Kaggle remains a snapshot. TEP is not interpolated to 1 s.

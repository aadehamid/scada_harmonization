# contextualize — Plane 3 (Phases 5b, 7)

UNS + IT transactional + ET topology → **identity reconciliation** (`ods_core.identity_map`:
stitching IT business keys ↔ OT/ISA-95 identities ↔ ET topology) → Neo4j knowledge graph
(ISO 15926 / DEXPI-aligned ontology, minimal start) → GraphRAG. CDC consumers (Python poll
→ Debezium milestone, Phase 5a) land here too. No code until Phase 5.
